
1)Used MVP patteren to implement the give requirement .which is good for TDD
2) Used Rx java in fetching the api.
3) used some of the libraries  Glide, ButterKnife , retrofit.
4) this sampleapp can be optimized more on the  sorting logicn when fav selected 
5) facebook and twiltter redirecting can be added .
6) used dummy data to create the app as the api was down from last week , today it was up .
